# Resume Prompt

Resume GridGuard strictly from repository-local state. Read `AGENTS.md` and its
required files in order, confirm `AGENT_STATE.json.current_milestone`, preserve
AuditOnly and all design-freeze boundaries, complete the current milestone with
tests/inspection evidence, update canonical state in the same commit, and continue
sequentially. Never execute or commit the reference binary.

